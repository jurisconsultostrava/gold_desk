services:
  mailroom:
    build: .
    image: mailroom:latest
    container_name: mailroom
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=5000
